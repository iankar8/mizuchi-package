// Stub for Supabase client library
window.supabase = {
  createClient: function(url, key) {
    console.log('Creating Supabase client with URL:', url);
    
    return {
      auth: {
        getSession: async function() {
          return { data: { session: null }, error: null };
        },
        signInWithPassword: async function(credentials) {
          return { data: { user: null }, error: "Auth functionality disabled in local stub" };
        },
        signUp: async function(credentials) {
          return { data: { user: null, session: null }, error: "Auth functionality disabled in local stub" };
        },
        signOut: async function() {
          return { error: null };
        }
      },
      from: function(table) {
        return {
          select: function(columns) {
            return {
              limit: function(n) {
                return {
                  eq: function(column, value) {
                    return { data: [], error: null };
                  }
                };
              },
              eq: function(column, value) {
                return { data: [], error: null };
              }
            };
          },
          insert: function(rows) {
            return { data: null, error: null };
          },
          upsert: function(rows) {
            return { data: null, error: null };
          },
          delete: function() {
            return {
              eq: function(column, value) {
                return { data: null, error: null };
              }
            };
          }
        };
      }
    };
  }
};

console.log('Supabase client library (local stub) loaded');